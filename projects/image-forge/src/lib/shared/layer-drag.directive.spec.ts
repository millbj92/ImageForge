import { LayerDragDirective } from './layer-drag.directive';

describe('LayerDragDirective', () => {
  it('should create an instance', () => {
    const directive = new LayerDragDirective();
    expect(directive).toBeTruthy();
  });
});
